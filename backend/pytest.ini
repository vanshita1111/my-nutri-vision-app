[pytest]
# pytest-asyncio 0.21+ requires explicit asyncio_mode.
# "auto" treats every async test/fixture as asyncio automatically,
# removing the need to mark each one with @pytest.mark.asyncio.
asyncio_mode = auto

# Show short tracebacks and live output for pipeline tests
addopts = --tb=short -q
testpaths = tests
